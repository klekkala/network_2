#include<stdio.h>



int runTimer(int timeout){
    pthread_t tid;
    int timePipe;
    Timestruct* tsp = malloc(sizeof(Timestruct));

    pipe(time);
}


int main(){

    FILE* conf;
    FILE* file;
    char buf[256];

    routingEntry* neighbor;
    fileEntry* Files;
    int num;


    //these are for ``
}


